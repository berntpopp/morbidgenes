# this is a dummy database config file, please copy this file to config.yml and replace variables accordingly
default:
  project: morbidgenes
db_setup:
  dbtype: mysql
  dbname: morbidgenes_db
  username: <replace with username>
  password: <replace with password>
  server: <replace with server ip>
  port: <replace with port>
  